<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="app.MeetingDetailPage"
             Title="MeetingDetailPage"
             Appearing="ContentPage_Appearing">

    <Grid RowDefinitions="50, *"
          Padding="10"
          RowSpacing="10">
        <Label Grid.Row="0" Text="Meeting Schedule" FontSize="32"/>
        <CollectionView
            Grid.Row="1"
            BackgroundColor="Black"
            x:Name="Sessions">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10"/>
                        </Border.StrokeShape>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="4*"/>
                            </Grid.ColumnDefinitions>
                            <Frame Grid.Column="0" CornerRadius="0">
                                <Label Text="{Binding DateStart, StringFormat='{0:dd/MM}'}" FontSize="20"/>
                            </Frame>
                            <Frame Grid.Column="1" CornerRadius="0">
                                <Label Text="{Binding SessionName}" FontSize="20"/>
                            </Frame>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>